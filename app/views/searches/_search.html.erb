<% if user_signed_in? %>
  <div class="search_form">
    <%= form_with url: search_path, local: true, method: :get do |f| %>
      <%= f.text_field :word %>
      <%= f.select :range, options_for_select([['👥レビュアー','User'], ['📕レビュー','Book']]) %>
      <%#= f.select :range, options_for_select([['会員','User'], [‘名盤’,’MasterPiece’]]) %> <!--([[入れ込む文字,Userモデル]-->
      <%= f.select :search, options_for_select([["完全一致", "perfect_match"], ["前方一致", "forward_match"], ["後方一致", "backward_match"], ["部分一致", "partical_match"]]) %>
      <%= f.submit "検索", class: "btn btn-outline-info btn-sm" %>
    <% end %>
  </div>
<% end %>